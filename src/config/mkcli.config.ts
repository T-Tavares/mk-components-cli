import type {Config} from '../types/types';

export const mkconfig: Config = {
    functionType: 'arrow',
    exportType: 'modular',
    css: true,
    cssModular: true,
    cssType: 'scss',
    cssAlias: 'ss',
};
